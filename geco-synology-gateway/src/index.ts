/**
 * Point d'entrée du GECO Synology Gateway.
 * Démarre le serveur Fastify local, la boucle de synchronisation
 * et le heartbeat vers la plateforme GECO (Cloudflare Worker).
 */
import "./server.js";
